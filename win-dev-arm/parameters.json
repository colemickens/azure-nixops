{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "newStorageAccountName": {
            "value": "${azname}storage"
        },
        "adminUsername": {
            "value": "${azusername}"
        },
        "dnsNameForPublicIP": {
            "value": "${azname}"
        },
        "VMName": {
            "value": "${azname}"
        },
        "adminPassword": {
            "value": "${azpassword}"
        },
        "location": {
            "value": "${azlocation}"
        },
        "vmSize": {
            "value": "${azvmsize}"
        }
    }
}
